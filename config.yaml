# Hailo Community Projects Configuration
# All other settings are managed by hailo-apps's own config system.

hailo_apps:
  url: "https://github.com/hailo-ai/hailo-apps.git"
  branch: "main"
  dir_name: "hailo-apps"
  # Development (internal):
  # url: "https://github.com/hailocs/hailo-apps-internal.git"
  # branch: "dev"
